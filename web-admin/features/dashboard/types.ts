export type DashboardMetric = {
  label: string;
  value: string;
  icon: string;
  detail: string;
};

export type RecentApplication = {
  company: string;
  role: string;
  status: string;
  date: string;
};
